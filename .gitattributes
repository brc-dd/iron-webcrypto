# Normalize line endings to LF and let git auto-detect text files.
* text=auto eol=lf

# Force source/config as text so binary heuristics never misclassify them.
*.ts text
*.js text
*.json text
*.md text
